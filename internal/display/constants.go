package display

const (
	// DefaultDisplayLimit is the default number of entries to show in the table
	DefaultDisplayLimit = 20

	// TableMaxRows is the maximum number of rows to display in the table
	TableMaxRows = 20

	// GraphWidth is the width of the ASCII graph in characters
	GraphWidth = 40

	// GraphHeight is the height of the ASCII graph in characters
	GraphHeight = 20

	// GoalHeaderWidth is the width for centering the goal header
	GoalHeaderWidth = 80

	// GoalLabelMinWidth is the minimum width for goal label padding
	GoalLabelMinWidth = 8
)
